dist/
staged/
zeroclaw/
__pycache__/
*.pyc
target/
*.wasm

# secrets — never commit env / credential files (per-repo .env lives symlinked
# to ~/Documents/secret; keep it out of the tree)
.env
.env.*
!.env.example
