name: level-17 already-signed input → DENY (unsigned invariant)
policy: merchant
intent: {action: transfer, amount_raw: "1000000000", recipient: RECIP}
tx: {kind: sol, amount: 1000000000, recipient: RECIP, signed: true}
expect: {verdict: DENY, reason_codes: [SH-DENY-SIGNED-001]}
