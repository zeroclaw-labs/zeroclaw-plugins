name = "account-balance"
version = "0.1.0"
description = "Inspect the lamport balance and RPC context of a public Solana account."
author = "Mateo Rojas"
wasm_path = "account_balance.wasm"
capabilities = ["tool"]
# http_client: one read-only getBalance request.
# config_read: reads only rpc_url and commitment.
permissions = ["http_client", "config_read"]
