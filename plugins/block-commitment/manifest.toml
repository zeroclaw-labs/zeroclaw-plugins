name = "block-commitment"
version = "0.1.0"
description = "Inspect cluster commitment stake for a public Solana slot."
author = "Mateo Rojas"
wasm_path = "block_commitment.wasm"
capabilities = ["tool"]
# http_client: one read-only getBlockCommitment request.
# config_read: reads only rpc_url and commitment.
permissions = ["http_client", "config_read"]
