name = "block-time"
version = "0.1.0"
description = "Resolve the estimated Unix production time for a public Solana slot."
author = "Mateo Rojas"
wasm_path = "block_time.wasm"
capabilities = ["tool"]
# http_client: one read-only getBlockTime request.
# config_read: reads only rpc_url and commitment.
permissions = ["http_client", "config_read"]
