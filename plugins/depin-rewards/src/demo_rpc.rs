//! Host-only reqwest-backed [`palinurus_core::Rpc`] for the demo driver (`--features demo`).
//!
//! Filled in slice 2 (TDD). Placeholder so the `demo` feature compiles while
//! the HTTP client slice lands first.

#![cfg(feature = "demo")]