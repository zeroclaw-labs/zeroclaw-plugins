name = "invoice-status"
version = "0.3.0"
description = "Check dual-rail invoice USDC settlement by Solana Pay reference (T0 read-only). PIX only if operator marks paid."
author = "ZeroClaw Solana Bounty"
wasm_path = "invoice_status.wasm"
capabilities = ["tool"]
# http_client: outbound Solana JSON-RPC (getSignaturesForAddress) via wasi:http.
# config_read: jailed plugin section (rpc_url, merchant_solana, usdc_mint).
permissions = ["http_client", "config_read"]
