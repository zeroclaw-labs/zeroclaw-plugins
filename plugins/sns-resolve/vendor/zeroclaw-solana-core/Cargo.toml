# AUTO-GENERATED by tools/sync-core.py — DO NOT EDIT.
# Canonical source: crates/solana-core (dev-dependencies stripped; the
# solana-sdk differential tests run there, on the host only).
# The Track E shared substrate: everything a ZeroClaw Solana tool plugin needs
# that `solana-sdk`/`solana-client` cannot provide inside a wasm32-wasip2 WIT
# component. Pure Rust, no I/O, no wasm dependency: HTTP is abstracted behind
# the `HttpTransport` trait so hosts test against mocks and wasm shims plug in
# `waki`. Consumed as a path dependency by the plugins in `plugins/solana-*`.
[package]
name = "zeroclaw-solana-core"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "wasm32-wasip2-friendly Solana substrate for ZeroClaw plugins: base58 pubkeys, PDA/ATA derivation, versioned-transaction construction, JSON-RPC shaping, durable nonces, SPL mint parsing."
publish = false

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bs58 = "0.5"
sha2 = "0.10"
base64 = "0.22"
# Off-curve check for PDA derivation only; no signing, no randomness.
curve25519-dalek = { version = "4", default-features = false, features = ["alloc"] }

