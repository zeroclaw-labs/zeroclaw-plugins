[package]
name = "solana-pay-request"
version = "0.1.0"
edition = "2021"
rust-version = "1.81"
license = "MIT OR Apache-2.0"
description = "ZeroClaw tool plugin for deterministic, zero-network Solana Pay requests"
repository = "https://github.com/zeroclaw-labs/zeroclaw-plugins"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nanosol = { git = "https://github.com/Fianko-codes/zeroclaw-solana.git", rev = "989cd0d3bd25ce6a2d796f72c0dc6a4ae56d989f" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(target_family = "wasm")'.dependencies]
wit-bindgen = "0.46"

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1

[workspace]
