name = "solana-token-risk"
version = "0.1.0"
description = "Offline rug/honeypot risk analysis for Solana SPL and Token-2022 mints from already-fetched RPC JSON"
author = "sasaki akira"
wasm_path = "solana_token_risk.wasm"
capabilities = ["tool"]
# T0 read-only analyzer: no network, no config, no signing. The host grants
# nothing beyond the base tool contract.
permissions = []
