name = "stake-account-brief"
version = "0.1.0"
description = "Read-only Solana stake schedule, validator health, delegation, and reward brief"
author = "teihin"
wasm_path = "stake_account_brief.wasm"
capabilities = ["tool"]
# http_client: JSON-RPC reads against the operator-configured Solana endpoint.
# config_read: reads only rpc_url and commitment from this plugin's jailed section.
permissions = ["http_client", "config_read"]
