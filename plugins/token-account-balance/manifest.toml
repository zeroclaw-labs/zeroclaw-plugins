name = "token-account-balance"
version = "0.1.0"
description = "Inspect the amount and decimals held by a public SPL token account."
author = "Mateo Rojas"
wasm_path = "token_account_balance.wasm"
capabilities = ["tool"]
# http_client: one read-only getTokenAccountBalance request.
# config_read: reads only rpc_url and commitment.
permissions = ["http_client", "config_read"]
