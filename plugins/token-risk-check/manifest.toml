name = "token-risk-check"
version = "0.1.0"
description = "Read-only Solana token risk check: authorities, concentration, LP status, and Token-2022 extensions."
author = "RugBuster"
wasm_path = "token_risk_check.wasm"
capabilities = ["tool"]
permissions = ["http_client", "config_read"]
